import org.apache.tools.ant.taskdefs.condition.Os

ext.osWin = Os.isFamily(Os.FAMILY_WINDOWS)
/*
 * This file was generated by the Gradle 'init' task.
 *
 * This is a general purpose Gradle build.
 * Learn more about Gradle by exploring our samples at https://docs.gradle.org/7.2/samples
 */

allprojects {
    group = 'org.opendcs'
    version = '8.0.0-M1'    
}

/*
 * TODO: Move the new things to the correct directories and ammend the merge commit.
 */

 

subprojects {
    apply plugin: 'base'
    
    tasks.withType(JavaCompile) {
        sourceCompatibility=JavaVersion.VERSION_11
        targetCompatibility=JavaVersion.VERSION_11
    }
}

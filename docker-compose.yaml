services:
  db:
    image: postgres:16-alpine # Using a lightweight Alpine-based image for PostgreSQL 16
    restart: always # Ensure the container restarts if it crashes
    environment:
      POSTGRES_USER: dcs_owner # Database username
      POSTGRES_PASSWORD: dcs_password # Database password
      POSTGRES_DB: dcs # Name of the database to create
    ports:
      - "5432:5432" # Map host port 5432 to container port 5432
    volumes:
      - db_data:/var/lib/postgresql/data # Persistent storage for database data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dcs_owner -d dcs -h localhost"]
      interval: 5s
      timeout: 3s
      retries: 20
      start_period: 10s
  migration:
    build:
      context: .
      target: tsdbapp
    environment:
      - DCS_OWNER=dcs_owner
      - DCS_OWNER_PASS=dcs_password
      - DCS_USER=app
      - DCS_PASS=app_pass
      - DCS_IMPLEMENTATION=OpenDCS-Postgres
      - DATABASE_URL=jdbc:postgresql://db:5432/dcs
      - placeholder_NUM_TS_TABLES=1
      - placeholder_NUM_TEXT_TABLES=1
    command: ["/schema.sh"]
    volumes:
      - ./compose_files/schema.sh:/schema.sh:ro
    restart: no
    depends_on:
      db:
       condition: service_healthy
  set-db-type:
    image: postgres:16-alpine # Using a lightweight Alpine-based image for PostgreSQL 16
    restart: no
    depends_on:
      migration:
        required: true
        condition: service_completed_successfully
    environment:
      - PGPASSWORD=dcs_password
    command: ["psql", "-U", "dcs_owner", "-w", "-f", "/extra.sql", "-h", "db", "-d", "dcs"]
    volumes:
      - ./compose_files/extra.sql:/extra.sql:ro
  api:
    build:
      context: .
      target: web-api
    entrypoint: ["/api_entry.sh"]
    command: ["/usr/local/tomcat/bin/catalina.sh", "run"]
    healthcheck:
      test: ["CMD-SHELL", "curl http://localhost:7000/odcsapi/health/live"]
      interval: 1m30s
      timeout: 30s
      retries: 5
      start_period: 30s
    ports:
      - 7000:7000
    volumes:
      - ./compose_files/api_entry.sh:/api_entry.sh:ro
      - ./compose_files/proxy_auth.sh:/proxy_auth.sh:ro
    environment:
      - DB_DRIVER_CLASS=org.postgresql.Driver
      - DB_USERNAME=app
      - DB_PASSWORD=app_pass
      - DB_VALIDATION_QUERY=SELECT 1
      - DB_URL=jdbc:postgresql://db:5432/dcs
      - DB_MAX_CONNECTIONS=10
      - DB_MAX_IDLE=5
      - DB_MIN_IDLE=1
    depends_on:
      migration:
        condition: service_completed_successfully
      set-db-type:
        condition: service_completed_successfully
  pgadmin:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: user@exaple.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    depends_on:
      db:
        condition: service_healthy
    restart: "unless-stopped"
    volumes:
      - ./compose_files/servers.json:/tmp/servers.json

  auth:
    image: quay.io/keycloak/keycloak:26.5
    command: ["start-dev", "--import-realm"]
    healthcheck:
      test: "/healthcheck.sh"
      interval: 5s
      timeout: 1s
      retries: 100
      start_period: 2s
    ports:
      - 7100:7100
    environment:
      - KEYCLOAK_ADMIN=admin
      - KEYCLOAK_ADMIN_PASSWORD=admin
      - KC_HEALTH_ENABLED=true
      - KC_FEATURES=admin-fine-grained-authz
      - KC_HTTP_PORT=7100
      - KEYCLOAK_FRONTEND_URL=http://localhost:7100
      - KC_HOSTNAME_STRICT=false
      - KC_PROXY=none
      - KC_HTTP_ENABLED=true
      - KC_HTTP_RELATIVE_PATH=/auth
    volumes:
      - ./compose_files/keycloak/realm.json:/opt/keycloak/data/import/realm.json:ro
      - ./compose_files/keycloak/healthcheck.sh:/healthcheck.sh:ro


volumes:
  db_data: # Define the named volume for data persistence
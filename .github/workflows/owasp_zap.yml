name: OWASP Zap
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  owasp-zap:
    name: Analyze with OWASP Zap API
    runs-on: ubuntu-20.04
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4.1.1
      - name: Set up JDK 17
        uses: actions/setup-java@v4.0.0
        with:
          java-version: 17
          distribution: 'temurin'
      - name: Cache Gradle packages
        uses: actions/cache@v3.3.3
        with:
          path: ~/.gradle/caches
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle') }}
          restore-keys: ${{ runner.os }}-gradle
      - name: Run OWASP Zap
        run: ./gradlew runOwaspZap
      - name: Generate GitHub Job Summary
        uses: x-color/github-actions-job-summary@v0.1.1
        with:
          file: ./opendcs-rest-api-jetty/build/test-results/owasp/zap_report.md
          vars: |
            empty: empty
      - name: docker-compose down
        if: always()
        run: ./gradlew stopOwaspZap

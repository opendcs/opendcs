version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"
  apt_packages:
    - plantuml
python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  fail_on_warning: false

formats:
  - pdf
  - epub
